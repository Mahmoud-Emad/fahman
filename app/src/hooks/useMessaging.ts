/**
 * useMessaging - Re-exports from MessagingContext for backward compatibility.
 * The messaging state is now managed globally via MessagingProvider.
 */
export { useMessaging, type UseMessagingReturn } from "@/contexts/MessagingContext";
